# Copy to .dev.vars (gitignored) for local `wrangler dev`, and set the same
# names as Worker secrets for deployment via `wrangler secret put <NAME>`.
# See SETUP.md for how to obtain each value. Never commit real values here.

CF_API_TOKEN=
CF_ACCOUNT_ID=
CF_DATABASE_ID=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
